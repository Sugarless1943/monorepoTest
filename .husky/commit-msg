#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# 校验提交信息格式
commit_msg=$(cat "$1")

# 检查是否以bugfix:, feat:, 或 hotfix: 开头
if ! echo "$commit_msg" | grep -qE '^(bugfix|feature|hotfix|release):'; then
  echo "错误: 提交信息必须以 bugfix:, feature:, hotfix:, 或 release: 开头"
  echo "示例: feature: 添加新功能"
  exit 1
fi
